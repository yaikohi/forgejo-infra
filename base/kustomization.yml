apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yml
  - deployment.yml
  - service.yml
  - ingress.yml # Optional, if you need external access

# Optional: Patches to customize the deployment
# patchesStrategicMerge:
#   - patches/deployment-resources.yml

# Optional: ConfigMaps or Secrets
# configMapGenerator:
# - name: forgejo-config
#   literals:
#     - FORGEJO_ROOT_URL=http://forgejo.example.com
# secretGenerator:
# - name: forgejo-secrets
#   literals:
#     - DB_PASSWORD=your_db_password